<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project03_timekeeping">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_e"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_e_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project03_timekeeping">
        <activity
            android:label="Time Keeping"
            android:name=".activity.ShowOneMemberActivity"
            android:exported="true" />
        <activity
            android:label="Time Keeping"
            android:name=".activity.ShowAllActivity"
            android:exported="true" />
        <activity
            android:label="Time Keeping"
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>